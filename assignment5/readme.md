# Assignment 5

## Changelog
Convert the application into a single-page application (SPA) using Bootstrap 5.

* Integrated **Bootstrap 5** framework for responsive design and UI components.
* Refactored navigation to use the Bootstrap Navbar, implementing a collapsible "hamburger" menu for mobile devices.
* Updated all JavaScript render functions to generate HTML using Bootstrap classes (Grid system, Cards, Alerts, Tables).
* Cleaned up custom CSS
* Improved visual feedback in the Statistics view using Bootstrap's contextual table classes (e.g., `table-success`, `table-warning`).
